import { buttonStyle } from "@/utils/styles";

const Button = () => {
  return (
    <button className={buttonStyle} type="submit">
      Submit
    </button>
  );
};

export default Button;
